const { trackQuizStart, trackQuizComplete, trackAffiliateClick } = useRoutineAnalytics();

// Tracks:
// - Quiz starts
// - Quiz completions (with answers)
// - Affiliate clicks (with product)
// - Quiz restarts